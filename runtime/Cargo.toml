[package]
name = "vex-runtime"
version = "0.1.0"
edition = "2021"
description = "Runtime nativo do Vex: generational references, alocação, drop ASAP"
license-file = "../LICENSE"

[lib]
crate-type = ["staticlib", "rlib"]
