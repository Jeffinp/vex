// Sem `fn main()`: top-level é o programa.
// Sem `let`: primeira atribuição declara.
// Sem `: int`: tipos inferidos.

def fib(n: int) -> int {
    if n <= 1 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

result = fib(10)
println(result)
